# Graph Visualizer - Task List
**Timestamp**: 2026-03-05_12-00

## 1. Project Setup
- [x] 1.1 Scaffold project with Vite: `npm create vite@latest . -- --template react-ts`
- [x] 1.2 Install React Flow: `npm install @xyflow/react`
- [x] 1.3 Clean up Vite boilerplate (remove default CSS, App content, assets)

## 2. Data Model
- [x] 2.1 Define `NodeData` and `EdgeData` TypeScript interfaces in `src/types.ts`
- [x] 2.2 Create sample graph data in `src/sampleData.ts` (5-6 nodes, 5+ edges with names)

## 3. Custom Node
- [x] 3.1 Create `src/components/CustomNode.tsx` — renders node name, styled with a CSS module
- [x] 3.2 Register `CustomNode` in the React Flow `nodeTypes` map

## 4. Graph Canvas
- [x] 4.1 Create `src/components/GraphCanvas.tsx` — wraps `<ReactFlow>` with nodes, edges, controls
- [x] 4.2 Enable `<Controls />` and `<Background />` from React Flow
- [x] 4.3 Wire `onNodeClick` callback to parent via prop

## 5. Node Detail Panel
- [x] 5.1 Create `src/components/NodeDetailPanel.tsx` — shows selected node name and all attributes as a key-value table
- [x] 5.2 Add close button to dismiss the panel
- [x] 5.3 Style as a side panel overlay with CSS module

## 6. Add Node / Edge Forms
- [x] 6.1 Create `src/components/AddNodeForm.tsx` — inputs: name + dynamic attribute rows
- [x] 6.2 Create `src/components/AddEdgeForm.tsx` — inputs: source node, target node, edge name
- [x] 6.3 Wire forms to `GraphPage` state (append to nodes/edges arrays)

## 7. GraphPage (Main Orchestrator)
- [x] 7.1 Create `src/pages/GraphPage.tsx` — holds nodes/edges state, selected node state
- [x] 7.2 Handle `onNodeClick` → set selected node → show NodeDetailPanel
- [x] 7.3 Render GraphCanvas + NodeDetailPanel + Add forms

## 8. App Entry
- [x] 8.1 Update `src/App.tsx` to render `<GraphPage />`
- [x] 8.2 Import React Flow base CSS in `main.tsx`

## 9. Polish
- [x] 9.1 Basic responsive layout (canvas fills viewport, panel overlays on right)
- [x] 9.2 Edge labels visible on the graph lines
- [x] 9.3 Verify pan/zoom works correctly
